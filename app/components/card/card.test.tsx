import React from "react";
import { render, screen } from "@testing-library/react";
import Card from "./card";

// it("renders the card as expected", () => {});

// perhaps some more specific render & behaviour tests...
